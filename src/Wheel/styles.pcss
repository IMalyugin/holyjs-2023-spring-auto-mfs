.component {
  & img {
    width: 100%;
    animation: rotate linear infinite .01s;
    transform-origin: 50% 50%;
    border-radius: 50%;
  }
}

@keyframes rotate {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}
